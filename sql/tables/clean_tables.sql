-- Generated from src/cleanstar/data_config.py; regenerate when definitions change.
-- Run after schemas.sql with CREATE TABLE privileges in the target schema.
-- IF NOT EXISTS preserves existing tables; it does not update their definitions.
-- Source types use CLEAN_COLUMN_TYPES; other source columns remain VARCHAR.

CREATE TABLE IF NOT EXISTS CLEANSTAR.CLEAN.BILLING_CLAIMS_CLEAN (
    CLAIM_ID VARCHAR,
    ENCOUNTER_ID VARCHAR,
    PATIENT_ID VARCHAR,
    PROVIDER_ID VARCHAR,
    ORGANIZATION_ID VARCHAR,
    PAYER_ID VARCHAR,
    INSURANCE_PLAN_NAME VARCHAR,
    MEMBER_ID VARCHAR,
    GROUP_NUMBER VARCHAR,
    RELATIONSHIP_TO_INSURED VARCHAR,
    PATIENT_FIRST_NAME VARCHAR,
    PATIENT_MIDDLE_INITIAL VARCHAR,
    PATIENT_LAST_NAME VARCHAR,
    PATIENT_SUFFIX VARCHAR,
    PATIENT_BIRTHDATE DATE,
    PATIENT_GENDER VARCHAR,
    PATIENT_ADDRESS VARCHAR,
    PATIENT_CITY VARCHAR,
    PATIENT_STATE VARCHAR,
    PATIENT_ZIP VARCHAR,
    PATIENT_PHONE VARCHAR,
    SERVICE_START_DATETIME TIMESTAMP_TZ,
    SERVICE_END_DATETIME TIMESTAMP_TZ,
    ENCOUNTER_CLASS VARCHAR,
    PROCEDURE_CODE VARCHAR,
    PROCEDURE_DESCRIPTION VARCHAR,
    DIAGNOSIS_CODE VARCHAR,
    DIAGNOSIS_DESCRIPTION VARCHAR,
    CLAIM_AMOUNT NUMBER(18, 2),
    PROVIDER_NAME VARCHAR,
    PROVIDER_NPI VARCHAR,
    PROVIDER_SPECIALTY VARCHAR,
    CLAIM_STATUS VARCHAR,
    CLAIM_CREATED_DATETIME TIMESTAMP_TZ,
    VALIDATION_ERROR_COUNT NUMBER(38, 0),
    VALIDATION_ERRORS VARCHAR,
    LOAD_RUN_ID VARCHAR,
    SOURCE_FILE VARCHAR,
    INGESTED_AT TIMESTAMP_TZ,
    SOURCE_ROW_HASH NUMBER(38, 0),
    VALIDATION_STATUS VARCHAR,
    VALIDATION_RULE_IDS VARCHAR,
    VALIDATION_MESSAGES VARCHAR
);

CREATE TABLE IF NOT EXISTS CLEANSTAR.CLEAN.ENCOUNTER_INFO_CLEAN (
    ENCOUNTER_ID VARCHAR,
    START_DATETIME TIMESTAMP_TZ,
    END_DATETIME TIMESTAMP_TZ,
    PATIENT_ID VARCHAR,
    ORGANIZATION_ID VARCHAR,
    PROVIDER_ID VARCHAR,
    ENCOUNTER_CLASS VARCHAR,
    ENCOUNTER_CODE VARCHAR,
    ENCOUNTER_DESCRIPTION VARCHAR,
    DIAGNOSIS_CODE VARCHAR,
    DIAGNOSIS_DESCRIPTION VARCHAR,
    BASE_ENCOUNTER_COST NUMBER(18, 2),
    LOAD_RUN_ID VARCHAR,
    SOURCE_FILE VARCHAR,
    INGESTED_AT TIMESTAMP_TZ,
    SOURCE_ROW_HASH NUMBER(38, 0),
    VALIDATION_STATUS VARCHAR,
    VALIDATION_RULE_IDS VARCHAR,
    VALIDATION_MESSAGES VARCHAR
);

CREATE TABLE IF NOT EXISTS CLEANSTAR.CLEAN.PATIENT_INFO_CLEAN (
    PATIENT_ID VARCHAR,
    BIRTHDATE DATE,
    FIRST_NAME VARCHAR,
    MIDDLE_INITIAL VARCHAR,
    LAST_NAME VARCHAR,
    SUFFIX VARCHAR,
    GENDER VARCHAR,
    ADDRESS VARCHAR,
    CITY VARCHAR,
    STATE VARCHAR,
    ZIP VARCHAR,
    PHONE VARCHAR,
    PAYER_ID VARCHAR,
    INSURANCE_PLAN_NAME VARCHAR,
    MEMBER_ID VARCHAR,
    GROUP_NUMBER VARCHAR,
    RELATIONSHIP_TO_INSURED VARCHAR,
    COVERAGE_START_DATE DATE,
    COVERAGE_END_DATE DATE,
    COVERAGE_STATUS VARCHAR,
    LOAD_RUN_ID VARCHAR,
    SOURCE_FILE VARCHAR,
    INGESTED_AT TIMESTAMP_TZ,
    SOURCE_ROW_HASH NUMBER(38, 0),
    VALIDATION_STATUS VARCHAR,
    VALIDATION_RULE_IDS VARCHAR,
    VALIDATION_MESSAGES VARCHAR
);

CREATE TABLE IF NOT EXISTS CLEANSTAR.CLEAN.PROVIDER_INFO_CLEAN (
    PROVIDER_ID VARCHAR,
    ORGANIZATION_ID VARCHAR,
    PROVIDER_NAME VARCHAR,
    SPECIALTY VARCHAR,
    NPI VARCHAR,
    LOAD_RUN_ID VARCHAR,
    SOURCE_FILE VARCHAR,
    INGESTED_AT TIMESTAMP_TZ,
    SOURCE_ROW_HASH NUMBER(38, 0),
    VALIDATION_STATUS VARCHAR,
    VALIDATION_RULE_IDS VARCHAR,
    VALIDATION_MESSAGES VARCHAR
);
